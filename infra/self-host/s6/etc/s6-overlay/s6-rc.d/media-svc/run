#!/bin/sh
# media-svc — stream-token mint + MediaMTX poll-bridge.
exec 2>&1
. /etc/pulse/env.sh
cd /opt/pulse/services/media-svc
exec /usr/sbin/gosu pulse /opt/pulse/venv/bin/uvicorn dcc_media_svc.app:app \
  --host 127.0.0.1 \
  --port 8004 \
  --no-access-log
