{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities the Pulse web app needs in the desktop shell — kept deliberately minimal: core defaults, native notifications, the global push-to-talk shortcut, the key/value store, and the GSR sidecar bridge (T3a). No shell, no filesystem.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default",
    "global-shortcut:default",
    "store:default",
    "allow-gsr-health",
    "allow-gsr-gpu-info",
    "allow-gsr-list-monitors",
    "allow-gsr-list-profiles",
    "allow-gsr-list-application-audio",
    "allow-gsr-build-argv",
    "allow-gsr-start",
    "allow-gsr-stop",
    "allow-gsr-state"
  ]
}
