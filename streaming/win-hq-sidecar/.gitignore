/target
/Cargo.lock
# Smoke-test artefacts (a 4K BMP is ~24 MB and uncompressed; don't commit).
capture_smoke_*.bmp
capture_smoke_*.png
audio_smoke_*.wav
# FFmpeg-LGPL-Distribution wird per `scripts/fetch-ffmpeg.ps1` reproduzierbar
# heruntergeladen (BtbN-Prebuilt, n8.1, ~88 MB Zip → ~250 MB ausgepackt). Pin
# steht im Script (URL + SHA256).
/ffmpeg-dist/
