[package]
name = "pulse-bildmarke"
version = "0.1.0"
edition = "2024"
publish = false

# Ohne Abhaengigkeiten — diese Crate wird von beiden Sidecars, vom Player und
# von pulse-whip eingebunden und darf deren Bauwege nicht beschweren.
[dependencies]
